export async function saveSubscription(
  subscriptionId: string,
  customerId: string
) {
  // Buscar usuário no FaunaDB com stripe_customer_id (user_by_stripe_customer_id)

  // Salvar dados da subscription do usuário no FaundaDB
}